-- Operational descriptor for the separately tested PartyTactics profile.
-- The original planned descriptor remains immutable and unavailable.
return {
    schema=1,
    key='sortie_objective_c_device_kill_v1',
    canonical_id='sortie-objective-c-device-kill-v1',
    version='1.0.0',
    available=true,
    note='Operator-directed, inert-load, stationary C-Device objective profile.',
}
