-- Isolated append-only identity for the Sortie Device C onboarding fight.
return {
    ordinal=12,
    id='sortie-objective-c-device-kill-v1',
    policy_id='pt-sortie-c-device',
    aliases={'sortiec','cdevice','sortie-c'},
}
